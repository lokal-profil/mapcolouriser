{
  "name": "mapcolouriser",
  "private": true,
  "type": "module",
  "description": "Dev-only Node toolchain for JS tests. The Flask app itself has no Node dependency.",
  "packageManager": "pnpm@10.33.0",
  "scripts": {
    "test": "vitest run"
  },
  "devDependencies": {
    "jsdom": "^25.0.0",
    "vitest": "^4.1.7"
  }
}
